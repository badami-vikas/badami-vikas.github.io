{
  "name": "zazoo-site",
  "private": true,
  "scripts": {
    "test": "node --test test/site.test.mjs test/worker.test.mjs",
    "brief:dev": "wrangler dev --config brief-api/wrangler.jsonc",
    "brief:deploy": "wrangler deploy --config brief-api/wrangler.jsonc"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "wrangler": "latest"
  }
}
